# Stage 02 results

Results are intentionally cleared while the two final Stage-02 screens are
rerun:

- L5/m=4 with NTP and fixed auxiliary targets `j=0,2,4,8`;
- L8/m=2 with NTP and fixed auxiliary targets `j=0,2,4,8`.

Both screens use 5,000 optimizer updates and record validation probes alongside
cross-entropy. New summaries and plots belong under each screen's run
directory after training.
